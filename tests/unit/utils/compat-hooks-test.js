import compatHooks from 'dummy/utils/compat-hooks';
import { module, test } from 'qunit';

module('Unit | Utility | compat hooks');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = compatHooks();
  assert.ok(result);
});
