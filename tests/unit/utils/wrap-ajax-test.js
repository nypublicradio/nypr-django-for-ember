import wrapAjax from 'dummy/utils/wrap-ajax';
import { module, test } from 'qunit';

module('Unit | Utility | wrap ajax');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = wrapAjax();
  assert.ok(result);
});
